# یکپارچه‌سازی ویزارد + پنل + استقرار خودکار از GitHub

## ⚠️ قدم صفر (الزامی): پروکسی api.cloudflare.com

مرورگر اجازه نمی‌ده جاوااسکریپت مستقیم با `api.cloudflare.com` حرف بزنه (CORS). برای همین
ویزارد از `proxy.js` به‌عنوان واسط استفاده می‌کنه. توکن با هدر سفارشی `X-CF-Token` فرستاده
می‌شه و خودِ `proxy.js` قبل از فوروارد به Cloudflare، اون رو به `Authorization: Bearer ...`
تبدیل می‌کنه؛ مسیرها هم از `/__cf/...` رد می‌شن (مثلاً `/__cf/accounts`).

دو راه برای دیپلویش هست:

### روش ۱ — همه‌چیز روی یک Worker (پیشنهادی، اصلاً CORS درکار نیست)
چون هم ویزارد و هم پروکسی از یک origin سرو می‌شن، اصلاً درخواست Cross-Origin وجود نداره.
```
wrangler deploy
```
(با همون `wrangler.toml` این ریپو — `main = proxy.js` و `[assets] directory = ./wizard`)
در این حالت فیلد «آدرس Worker پروکسی» توی تنظیمات ویزارد رو **خالی بذار**.

### روش ۲ — ویزارد روی GitHub Pages بمونه، فقط proxy.js جدا دیپلوی بشه
یک‌بار، دستی، بدون CLI:
1. `dash.cloudflare.com` → `Workers & Pages` → `Create Worker` → اسمی مثل `cf-proxy` → Deploy
2. `Edit code` → کل کد پیش‌فرض رو پاک کن → محتوای `proxy.js` رو جایگزینش کن → Deploy
3. آدرسی که می‌گیری (`https://cf-proxy.YOUR-SUBDOMAIN.workers.dev`) رو توی ویزارد →
   **تنظیمات → آدرس Worker پروکسی** وارد کن و ذخیره کن.

بعد از هر کدوم از این دو روش، بقیه‌ی ویزارد (توکن، دیپلوی، داشبورد، آپدیت، حذف) کار می‌کنه.

## چه چیزی اضافه شد

1. **دکمه‌ی «استقرار نمونه جدید (ویزارد)»** داخل پنل مدیریت (منوی «به‌روزرسانی پـنـل»).
   با کلیک، همان ویزارد گیت‌هاب‌پیجزتان در تب جدید باز می‌شود — بدون نیاز به خروج از پنل.
   آدرس این لینک از یک binding به اسم `WIZARD_URL` خوانده می‌شود.

2. **دیپلوی خودکار روی Push** (`.github/workflows/deploy-worker.yml`):
   هر بار که `Source.js` را در ریپو تغییر دهید و به شاخه‌ی `main` پوش کنید،
   یک GitHub Action اجرا می‌شود که دقیقاً کاری را انجام می‌دهد که دکمه‌ی
   «آپدیت خودکار» داخل پنل انجام می‌دهد: bindingهای فعلی هر Worker (D1، KV و...)
   را نگه می‌دارد و فقط کد را با نسخه‌ی تازه جایگزین می‌کند. یعنی ساب‌لینک(های)
   از قبل دیپلوی‌شده هم به‌روز می‌شوند، بدون این‌که لازم باشد کسی وارد پنل شود.

## کارهایی که باید شما انجام دهید (یک‌بار)

### الف) ست کردن Secretهای ریپو
در گیت‌هاب: `Settings → Secrets and variables → Actions → New repository secret`

| نام Secret         | مقدار                                                                 |
|--------------------|------------------------------------------------------------------------|
| `CF_API_TOKEN`     | همان توکنی که با ویزارد ساختید (دسترسی Workers Scripts + D1 + Account Settings) |
| `CF_ACCOUNT_ID`    | Account ID کلودفلرتان                                                  |
| `CF_WORKER_NAMES`  | نام Worker(ها)، اگر چند نمونه دارید با کاما جدا کنید. مثال: `miladconfig-worker,miladconfig-worker2` |

⚠️ چون یک‌بار توکن را داخل چت پیست کردید، پیشنهاد می‌شود همان توکن قبلی را از داشبورد کلودفلر Revoke کنید و یک توکن تازه بسازید — همان توکن تازه را اینجا هم بگذارید.

### ب) هماهنگ کردن آدرس ویزارد
اگر آدرس واقعی GitHub Pages شما با `https://miladjahani.github.io/wizard245/` فرق دارد:
- ساده‌ترین راه: در مرحله ۲ ویزارد، فیلد «آدرس همین ویزارد در GitHub Pages» را با آدرس درست پر کنید و یک‌بار Worker را دوباره استقرار/آپدیت بدهید تا binding درست شود.
- یا دستی: در داشبورد Cloudflare → Worker موردنظر → Settings → Variables → یک متغیر متنی به اسم `WIZARD_URL` با آدرس درست اضافه کنید.

## جریان کامل از این پس

```
تغییر در Source.js  →  git push به main
        │
        ├─▶ Actions: Deploy MiladConfig Wizard to GitHub Pages   (اگر wizard/** تغییر کرده بود)
        └─▶ Actions: Deploy Source.js to Cloudflare Workers      (چون Source.js تغییر کرده)
                        │
                        ▼
        هر Workerِ لیست‌شده در CF_WORKER_NAMES به‌روزرسانی می‌شود
        (bindingهای D1/KV دست‌نخورده می‌مانند، فقط کد عوض می‌شود)
```

اگر یکی از نام‌های داخل `CF_WORKER_NAMES` هنوز دیپلوی نشده باشد، آن یکی را با یک هشدار رد می‌کند و بقیه را ادامه می‌دهد (خطای یک نمونه، بقیه را متوقف نمی‌کند).
